# Place Salesforce Project Here

Use this folder for a real Salesforce DX project, or document the external project path in `README.md`.

Expected real metadata root:

```text
force-app/main/default/
```

Common real project paths after copying a project here:

```text
FORCE_APP_DIRECTORY/my-salesforce-project/force-app/main/default/classes/
FORCE_APP_DIRECTORY/my-salesforce-project/force-app/main/default/lwc/
FORCE_APP_DIRECTORY/my-salesforce-project/force-app/main/default/objects/
```

Do not create placeholder deployable Salesforce metadata in this folder. Codex must work only against the real Salesforce DX project the user provides.
