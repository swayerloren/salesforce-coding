# Project Memory

## Repo Structure

- This repo is a Codex-ready Salesforce coding engine, not automatically the user's Salesforce DX project.
- Salesforce knowledge lives under `SALESFORCE_KNOWLEDGE/`.
- The user's real Salesforce DX project should live under `FORCE_APP_DIRECTORY/` or be externally referenced there.
- Codex must locate a real `force-app/main/default` folder before editing Salesforce source.
- `FORCE_APP_DIRECTORY/` is placeholder-only until the user adds or references a real Salesforce DX project; do not create placeholder deployable Apex, LWC, object, layout, or metadata files there.
- Root `README.md` now presents the repo as a Codex-ready Salesforce coding engine/helper, with setup flow, project placement, knowledge map, memory/history behavior, and safety rules.
- `SALESFORCE_KNOWLEDGE/INDEX.md` is the master Salesforce knowledge map and includes the task-specific reading map Codex should use before Salesforce edits.

## Codex Operating Rules

- Codex must read `START_HERE.md` and `INSTRUCTIONS/` before task work.
- Codex must inspect existing files before editing.
- Codex must not guess Salesforce object, field, metadata, permission, profile, record type, Apex method, or component names.
- Codex must make the smallest safe change and avoid unrelated edits.
- Meaningful work should update `MEMORY/PROJECT_MEMORY.md` and `HISTORY/TASK_HISTORY.md`.
- Specialized memory files now exist for reusable fix patterns, failure prevention rules, architecture decisions, stable project notes, and verified org patterns.
- Specialized history files now exist for Codex run logs, deployment logs, test result logs, and larger completed change archives.
- Documentation governance now lives at `SALESFORCE_KNOWLEDGE/DOCS/documentation-governance.md`; it defines source authority, documentation precedence, conflict handling, duplicate handling, and update placement.
- Project discovery support now lives at `SALESFORCE_KNOWLEDGE/REFERENCE/project-discovery-template.md` and `SALESFORCE_KNOWLEDGE/CHECKLISTS/project-discovery.md`; use it before broad or unfamiliar Salesforce DX project work.
- Public docs must not depend on temporary reference folders. Useful lessons from temporary material should be generalized into public-safe rules, templates, checklists, memory, or history.
- `WIKI_DRAFTS/` stores Markdown source drafts that the repo owner can copy into the GitHub Wiki. These pages explain the same operating model as the repo: users provide a real Salesforce DX project, Codex locates `force-app/main/default`, reads `INSTRUCTIONS/` and `SALESFORCE_KNOWLEDGE/`, then makes narrow validated changes.
- `SALESFORCE_KNOWLEDGE/PROMPTS/CODEX_PROMPT_PACK/` stores complete copy-paste-ready Codex prompts for common Salesforce task types. Every prompt reinforces the required workflow: read `START_HERE.md`, read `INSTRUCTIONS/`, locate the real project, read `SALESFORCE_KNOWLEDGE/INDEX.md`, inspect real `force-app/main/default` files, make the smallest safe fix, avoid private data, return complete updated files when code changes, and update `MEMORY/` and `HISTORY/`.
- `SALESFORCE_KNOWLEDGE/CHECKLISTS/CODEX_ENGINE_CHECKLISTS/` stores strict preflight checklists for Codex tasks, Apex edits, LWC edits, metadata edits, deployment, GitHub push readiness, and public repo safety.
- Path/link consistency review on 2026-04-27 updated stale release-note and wiki guide references to the `SALESFORCE_KNOWLEDGE/` structure. Historical old-path references remain only in workspace audit context or as valid relative links inside `SALESFORCE_KNOWLEDGE/`.
- Public release safety review on 2026-04-27 found no blocking credentials, tokens, org IDs, private data, local-only path dependencies, temporary-folder dependencies, screenshots, private logs, or accidental real Salesforce metadata in `FORCE_APP_DIRECTORY/`. Owner should still confirm the final README badge/release repository URL and whether `WORKSPACE/` audit reports should be published.
- Final polish pass on 2026-04-27 standardized public docs around the terms Codex-ready Salesforce coding engine, Salesforce DX project, `force-app/main/default`, Salesforce knowledge base, Memory, History, real project metadata, and public-safe.
- GitHub update planning on 2026-04-27 found this working folder is not currently a git repository. `WORKSPACE/GITHUB_UPDATE_PLAN.md` records suggested commit/release text, moved/added/changed file groups, manual checks, and confirms no push was performed.
- New-user usability review on 2026-04-27 found the repo understandable for a brand-new user. Prompt visibility was improved by adding common-task prompt tables to `README.md` and `START_HERE.md`, prompt shortcuts to `SALESFORCE_KNOWLEDGE/INDEX.md`, and common starting points to `WIKI_DRAFTS/Home.md`.
- Final push readiness verification on 2026-04-27 passed and is recorded in `WORKSPACE/PUSH_READY_REPORT.md`. The report marks the repo `READY TO PUSH`, with remaining manual checks for final GitHub URL, workspace publishing intent, Mermaid rendering, and GitHub secret scanning.
- Release documentation for the Codex-ready Salesforce workspace engine restructure is tracked as `v1.1.0`, not `v2.0.0`, because it is a major workspace and documentation improvement without a packaged API or runtime contract break.
- `RELEASE_NOTES_v1.1.0.md` documents the pushed restructure commit `f973f5bbe555f0ec5bb0e9ebe0365000b82ba9ac`, including moved root Salesforce knowledge assets, the new instruction system, `FORCE_APP_DIRECTORY/`, Memory, History, wiki drafts, prompt pack, checklist pack, public-safety checks, and migration notes for users coming from `v1.0.0`.
- Deep repo quality audit on 2026-04-27 scored the repo 82/100 and found two P0 gaps before heavy public promotion: `v1.1.0` release docs should align with the actual tag/current release-doc commit, and `FORCE_APP_DIRECTORY/` needs stronger ignore/local-pointer guardrails to prevent accidental private metadata or local path commits.
- External tooling integration layer added `TOOLS/`, `QUALITY_GATES/`, `AUTOMATION/`, and `VENDOR_REFERENCES/` as documentation/script scaffolding only. External Salesforce tooling and sample repos must be referenced by URL with attribution and must not be vendored into this repo.
- Local validation scripts in `AUTOMATION/` are now strict project-discovery helpers for Codex. They locate the real Salesforce DX project from an explicit path, `FORCE_APP_DIRECTORY/`, a filled `FORCE_APP_DIRECTORY/README.md` external path note, or repo-root `force-app/main/default`; they report `FOUND`, `MISSING`, `SKIPPED`, `PASS`, and `FAIL`; optional missing tools are not blocking failures; deployment and org login are never required.
- GitHub Actions workflow drafts now live in `.github/workflows/`: `markdown-link-check.yml`, `public-safety-check.yml`, and `salesforce-code-analyzer.yml`. They require no secrets by default; the Code Analyzer workflow skips when no `force-app/main/default` is found and analyzes local source only when metadata exists.
- External Apex Recipes and LWC Recipes maps now live in `SALESFORCE_KNOWLEDGE/REFERENCE/`. Codex may use them as public pattern references only after inspecting the user's real `force-app/main/default`; recipe object, field, component, event, and Apex names must not be invented or copied into a user project.
- Salesforce Code Analyzer is a first-class quality gate. After Apex, LWC, Aura, metadata, Flow, static resource, or deployment-scope changes, Codex should run it when available or state the exact skipped reason. Codex must not claim static analysis passed unless `sf code-analyzer run` or an equivalent analyzer command actually ran and passed.
- LWC quality gates are now stronger. After LWC changes, Codex should run or recommend existing LWC Jest, LWC ESLint, and mobile lint scripts when available; it must report missing `package.json`, Node/npm, dependencies, or scripts as `SKIPPED` or `MISSING`.
- Codex must not claim LWC Jest, ESLint, mobile lint, or graph analyzer passed unless the exact command ran and exited successfully.
- For Salesforce mobile tasks, mobile behavior is a required review area. Codex should inspect LWC bundle files, `js-meta.xml`, FlexiPage/action placement, CSS, Apex controllers, and permissions, and use mobile lint or graph analyzer when configured.
- LWC templates must remain declarative: computed template state belongs in JavaScript getters, not inline JavaScript expressions in HTML.
- Apex output quality gates now require explicit checks for bulk safety, no SOQL/DML in loops, no hard-coded Salesforce IDs, null-safe handling, sharing/security intent, test-created data, and real assertions.
- Apex Recipes remains a pattern reference only. Codex may learn shapes for services, async Apex, security, test factories, and assertions, but must not copy recipe object, field, class, permission, or metadata names into a user's project.
- Optional Apex formatting uses `prettier-plugin-apex` only when already configured or user-approved. Codex must not run broad `--write` formatting during unrelated Apex fixes.
- After Apex production changes, Codex should run Salesforce Code Analyzer when available or state the exact skipped reason. Codex must not claim Apex static analysis passed without an actual passing analyzer run.
- Metadata work now requires full source-format and activation-chain inspection. Codex must verify `force-app/main/default`, inspect existing metadata before editing, preserve object-scoped paths, and avoid inventing object, field, layout, FlexiPage, action, permission, profile, record type, tab, app, report, dashboard, or static resource names.
- Record-page behavior requires activation, assignment, permissions, record type, app/profile/form-factor, Dynamic Forms, Dynamic Actions, layout, compact layout, and quick-action checks. A component can deploy successfully and still not appear to users.
- Profiles are high-blast-radius metadata. Codex should prefer permission sets when project conventions allow, avoid blind profile edits, and avoid wide metadata payloads or wildcard package manifests unless owner-approved.
- The Salesforce project validators now include conservative metadata path checks for objects, fields, validation rules, record types, compact layouts, layouts, FlexiPages, quick actions, permission sets, profiles, tabs, applications, custom metadata, custom permissions, static resources, email, reports, dashboards, and package manifests.
- Optional external Salesforce reference repos can be cloned locally under `VENDOR_REFERENCES/_external/` using the PowerShell or Bash helper scripts. That folder is gitignored and must not be committed.
- Codex may inspect optional local vendor clones if present, but they are not required, are not public repo dependencies, and must not be copied blindly. Real user project metadata under `force-app/main/default` remains the source of truth.
- Tooling integration consistency review on 2026-04-28 aligned public docs, wiki drafts, prompt pack references, workflow skip behavior, and automation safety scans with the new `TOOLS/`, `QUALITY_GATES/`, `AUTOMATION/`, and `VENDOR_REFERENCES/` layers. Local quality scans must skip optional vendor clones under `VENDOR_REFERENCES/_external/`, `temp/`, and dependency folders to avoid treating external source as this repo's public content.
- Release documentation for the tooling/reference/quality gate integration is tracked as `v1.2.0`, a backward-compatible minor release. It documents commit `3f321c1f7316eb35ecb44dee6c17f49999b68308`, which added the tooling layer, quality gates, local automation, optional vendor reference system, workflow drafts, external recipe maps, and strengthened Codex validation behavior.
- External intelligence strategy is now implemented as original public-safe documentation layers. `SALESFORCE_KNOWLEDGE/REFERENCE/EXTERNAL_INTELLIGENCE/` stores distilled strategies from public external repos; `SALESFORCE_KNOWLEDGE/COMMANDS/` stores command maps; `SALESFORCE_KNOWLEDGE/PARAMETERS/` stores option maps; `SALESFORCE_KNOWLEDGE/QUALITY_STRATEGIES/` stores task-specific quality posture; and `SALESFORCE_KNOWLEDGE/VALIDATION_FLOWS/` stores ordered Codex validation sequences.
- Codex should read command maps, parameter maps, quality strategies, and validation flows after task-specific Salesforce knowledge and before selecting validation commands. These docs guide what to run and when, while `AUTOMATION/` remains the local wrapper layer for project discovery and safe tool detection.
- External repos remain intelligence sources only. Do not copy external source, sample metadata, generated artifacts, workflow files, `.git` folders, or `node_modules` into this repo. Preserve attribution by URL and verify commands against installed tools or upstream docs when uncertain.
- Release documentation for the external Salesforce tooling intelligence integration is tracked as `v1.3.0`, a backward-compatible minor release. It documents implementation commit `c15aa299ee8312a46813e2fa8e1811c2014b027f`, the new external intelligence docs, command maps, parameter maps, quality strategies, validation flows, prompt/checklist updates, and no-vendoring validation.
- Batch 1 P0 Salesforce technical gaps are implemented. New reusable rules now cover sharing/CRUD/FLS/user-mode/system-mode security posture, trigger order of execution and Flow re-entry, Files lifecycle across `ContentVersion`/`ContentDocument`/`ContentDocumentLink`, destructive deployment safety, debug-log triage, compound address leaf fields, and Chatter/email/activity automation. Future Salesforce tasks should read the focused topic docs before changing related Apex, metadata, Files, deployment, or communication behavior.
- Public-safe extraction rule reinforced: local private references are evidence only. Convert them into generic Salesforce rules, checklists, anti-patterns, and validation flows; do not store private names, message bodies, raw logs, file names, screenshots, org IDs, local paths, or external source code in public docs, Memory, or History.
- Batch 2 LWC/mobile/Files/Visualforce gaps are implemented. Future LWC work should read the wire/imperative/cache rules, dynamic-record-field rules, navigation/toast/refresh patterns, LWC quality strategy, and Fix LWC flow before editing bundles.
- Future mobile Salesforce work should inspect the full action visibility chain: component metadata, action metadata, layout actions, Dynamic Actions, FlexiPage activation, permissions, form factor, and runtime surface. A deployed action or component is not proof it appears in the mobile action bar.
- Future file/photo/PDF work should distinguish upload, preview, download, latest version, generated artifact creation, parent refresh, and external/mobile handoff. `ContentVersion`, `ContentDocument`, `ContentDocumentLink`, and optional distribution/external URLs have separate responsibilities.
- Future Visualforce PDF work should preserve stable output contracts, inspect page/controller/static-resource dependencies, avoid modern-browser assumptions, and create durable Salesforce Files when users need later mobile preview, download, sharing, or audit access.
- Batch 3 deployment/debugging/CLI gaps are implemented. Future deployment work should distinguish static review, local tool pass, Apex test pass, dry-run deploy pass, production validation pass, quick deploy pass, and real deploy pass. Do not report success without the exact evidence category.
- Future Salesforce CLI work should verify installed help before mutating or source-writing commands. Use `project deploy start --dry-run` for ordinary dry-run validation, `project deploy validate` for production quick-deploy workflows, `project deploy report` for async or timed-out deploy status, and `project deploy quick` only after a successful validation job.
- Future destructive deployment work requires separate ordinary and destructive manifests, exact metadata type/full name review, dependency inspection, validation-only evidence, rollback notes, and explicit owner approval before real deletion. Do not use wildcard destructive members or default to purge-on-delete.
- Future Code Analyzer guidance should use current `sf code-analyzer` command families. Retired scanner command shapes should not be added to new docs, scripts, or CI workflows.
- Debug logs remain evidence to summarize, not content to publish. Deployment and debug reports should record entry point, command family, scope, test level, failing layer, public-safe root cause, fix, validation, and private output omitted.
- Batch 4 prompt/checklist/Codex behavior controls are implemented. Future Codex prompt and checklist work should enforce reading `START_HERE.md`, locating the real `force-app/main/default`, reading command maps, parameter maps, quality strategies, and validation flows, inspecting current files before editing, avoiding guessed API names and invented metadata, avoiding unrelated edits and broad refactors, using external repo intelligence as learning-only, updating Memory/History, preserving public safety, and never claiming tests, lint, analyzer, deploy, retrieve, GitHub, or runtime checks passed without exact passing evidence.
- Batch 5 onboarding polish is implemented. New-user docs now consistently explain the release ZIP workflow: download, extract, open in VS Code with Codex, place or reference the real Salesforce DX project, tell Codex to read `START_HERE.md`, route Codex through instructions, knowledge, command maps, parameter maps, quality strategies, validation flows, prompts, and checklists, inspect current files, make narrow fixes, run or recommend validation, and update Memory/History.
- Final knowledge improvement audit on 2026-04-28 scored the repo at 109/120 (91%) after Batch 1 through Batch 5, up from 77/120 (64%) in the master gap matrix. Content is ready for owner review and commit from a real git checkout, but this downloaded workspace has no `.git` directory. Recommended next version is `v1.4.0` for the knowledge and onboarding expansion.
