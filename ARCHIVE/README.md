# Archive

This folder stores superseded or historical material that should not be treated as current instructions.

Archived content should remain public-safe. Do not archive private raw notes, secrets, credentials, deploy artifacts, local auth files, or local absolute paths.

Current archive areas:

- `OLD_FIXES/`: old fix notes after useful lessons have been extracted into current knowledge or history.

